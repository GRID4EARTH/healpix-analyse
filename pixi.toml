[workspace]
authors = ["Jean-Marc Delouis"]
channels = ["conda-forge"]
name = "healpix-analyse"
platforms = ["linux-64", "osx-arm64"]
version = "2026.8.1"

[tasks]

[dependencies]
numpy = ">=2.4.2,<3"
python = ">=3.10,<3.14"
jupyterlab = ">=4.5.5,<5"
pytorch = ">=2.0,<3"
pyproj = ">=3.6"
healpy = ">=1.16"


[pypi-dependencies]
healpix-analyse= { path = ".", editable = true }
# Validate against the shared upstream integration branch while developing PR #37.
# Keep pyproject.toml on the released dependency so normal installations are
# not tied to an unreleased branch.
healpix-geo = { git = "https://github.com/GRID4EARTH/healpix-geo.git", branch = "integration", subdirectory = "python/healpix-geo" }

[feature.affine.dependencies]
affine = "*"

[feature.test.dependencies]
pytest = "*"
pytest-cov = "*"

[feature.docs.dependencies]
fftw = "*"
sphinx = "*"
pydata-sphinx-theme = "*"
myst-nb = "*"
sphinx-autobuild = "*"
sphinx-autoapi = "*"
sphinx-design = "*"
matplotlib-base = ">=3.7,<4"
pandas = "*"
tabulate = "*"

[feature.test.tasks]
tests = { cmd = "pytest --cov=healpix_analyse" }

[feature.docs.tasks]
build-docs = { cmd = "rm -rf generated/; set +e; python -m sphinx -b html -w warnings.log -W -Tn -j auto . _build; sphinx_status=$?; set -e; cat warnings.log; exit $sphinx_status", cwd = "docs" }
autobuild-docs = { cmd = "sphinx-autobuild -b html -w warnings.log -W -Tn -j auto . _build", cwd = "docs" }

[feature.dev.dependencies]
ipython = "*"
jupyterlab = "*"
black = "*"
jupyter-resource-usage = "*"
jupyterlab_code_formatter = "*"
isort = "*"


[environments]
default = ["test", "dev", "affine"]
docs = ["docs"]
